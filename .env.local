DOTENV_CONFIG_QUIET=true
# ── Database ─────────────────────────────────────────────────
# PostgreSQL (default)
# DATABASE_URL="postgresql://user:password@localhost:5432/mydb?schema=public"

# MySQL         → also change prisma/schema.prisma provider to "mysql"
DATABASE_URL="mysql://root@localhost:3306/iffi_festival_api_v2"

# SQLite        → also change prisma/schema.prisma provider to "sqlite"
# DATABASE_URL="file:./dev.db"

# MongoDB       → also change prisma/schema.prisma provider to "mongodb"
# DATABASE_URL="mongodb+srv://user:password@cluster.mongodb.net/mydb"

# ── Server ───────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── CORS ─────────────────────────────────────────────────────
# Comma-separated allowed origins (leave empty to allow all in dev)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ── Rate Limiting ────────────────────────────────────────────
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# CAPTCH SECRET KEY
# RECAPTCHA_SECRET_KEY=6LeIBwkqAAAAAOJg5NYWSIfl_aqo3ShYGpgxyIu3
RECAPTCHA_SECRET_KEY=6Lc5sgsqAAAAACCWISUApVWPR5HgxisIhoivgNA8

# MAIL SETUP
MAIL_HOST=smtp.gmail.com
MAIL_DRIVER=smtp
MAIL_PORT=587
MAIL_FROM_ADDRESS=digitalteam@nfdcindia.com
MAIL_FROM=Digital-Team
MAIL_ENCRYPTION=tls
MAIL_USERNAME=digitalteam@nfdcindia.com
MAIL_PASSWORD=jxifvetskgsfqrgk

# JWT
JWT_SECRET=welcome-to-iffi-enterprise-new-version
JWT_EXPIRES_IN=7d
BCRYPT_ROUNDS=10
BCRYPT_SALT_ROUNDS=12

# FRONT-END URL
FRONTEND_URL=https://iffigoa.org/festival